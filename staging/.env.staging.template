# Wactorz staging — copy to .env.staging and fill in values
TZ=UTC
MONITOR_PORT=8888
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_WS_PORT=9001
# Optional: server-side TTS (requires edge-tts, already bundled via [all])
TTS_VOICE=en-US-JennyNeural
